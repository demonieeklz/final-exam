apiVersion: v1
kind: Pod
metadata:
  labels:
    author: dilmurod
    last: final
  name: nginx2
  namespace: dilmurod-final
spec:
  containers:
  - image: nginx
    imagePullPolicy: Always
    name: nginx2
    resources: {}
